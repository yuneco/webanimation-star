export class StarAnimOption {
  constructor (duration, vec, scale = 1, angle = 0, alpha = 1) {
    this.duration = duration;
    this.vec = vec;
    this.scale = scale;
    this.angle = angle;
    this.alpha = alpha;
  }
}

